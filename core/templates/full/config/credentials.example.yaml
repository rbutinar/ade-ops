# Template — copy this file to credentials.yaml and fill in your values.
# credentials.yaml is gitignored. Never commit it.

databricks:
  host: "{workspace_url}"
  token: "${DATABRICKS_TOKEN}"   # env var reference (recommended)
  # token: "dapi..."             # or direct value (local only)

# fabric:
#   tenant_id: "{tenant_id}"
#   # auth_method: how to acquire the Fabric REST API token. One of:
#   #   - service_principal — requires client_id + client_secret below (best for CI)
#   #   - az_cli            — uses `az account get-access-token` (best for local dev)
#   #   - device_code       — interactive MSAL flow (fallback)
#   # Omit to auto-detect: SP if fully configured, else az_cli if available, else device_code.
#   auth_method: az_cli
#   # client_id: "${FABRIC_CLIENT_ID}"
#   # client_secret: "${FABRIC_CLIENT_SECRET}"

# fabric_warehouse:
#   server: "{workspace_endpoint}.datawarehouse.fabric.microsoft.com,1433"
#   database: "{warehouse_name}"
#   tenant_id: "{tenant_id}"
#   # auth_method:
#   #   - msal              — interactive browser + persistent cache (best for local dev)
#   #   - service_principal — requires client_id + client_secret (best for CI)
#   auth_method: msal
#   # client_id: "${FABRIC_WH_CLIENT_ID}"
#   # client_secret: "${FABRIC_WH_CLIENT_SECRET}"
#   # login_hint: "user@tenant.com"  # pre-fill the browser email field
